function convertir_csv(ruta,nodo){ //convertir csv a json y subir informacion a firebase

   //get nombre tabla
    var messagesRef = new Firebase("https://pruebas-csv.firebaseio.com/"+nodo); 
console.log('{{storage_path("app/employees/3/profile/image.png")}}' );
   //d3.tsv(ruta, function(data) {
    
}